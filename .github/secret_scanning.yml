# The shared compliance preset requires this file, while GitHub's schema
# requires at least one path. Limit the exclusion to this config itself;
# Gitleaks and Secretlint continue to scan it locally and in CI.
paths-ignore:
    - ".github/secret_scanning.yml"
